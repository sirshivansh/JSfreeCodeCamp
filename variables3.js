// variables which are declared with "const" keyword cannot be reassigned like those with "let" keyword
// "const" variables cannot change their values
// a "const" variable cannot be uninitialized

const character = "Hello";
const count = 8;
const rows = [];

console.log(character);
console.log(count);
console.log(rows);